# NIRU MVP — Environment variable template
# Copy this file to .env and fill in your values.
# .env is gitignored and should never be committed.

# GitHub secret name: HODI_AIGEMINI — mapped to GOOGLE_API_KEY in the app
GOOGLE_API_KEY=your_google_api_key_here
NIRU_WHISPER_MODEL=small
NIRU_LIBRARY_DIR=H:/
